spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mdd
    username: ad
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
